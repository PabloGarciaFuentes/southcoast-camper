[build]
  base = "frontend"                      # Build desde frontend
  command = "npm install && npm run build"
  publish = ".next"                      # Relativo al base
  functions = ".netlify/functions"       # Serverless functions para API Routes

[[plugins]]
  package = "@netlify/plugin-nextjs"
